# Visp AI Translator - 交互流程指南

## 📱 当前交互方式

### 1. 语音翻译模式 (Alt+V)

#### 完整流程

```
用户操作          系统响应                    UI 反馈
─────────────────────────────────────────────────────────
按下 Alt+V    →  后端监听到按键              Island 出现
              →  发送 voice-mode-start      显示 "🔴 Listening..."
              →  开始录音                    波形动画开始

说话中...     →  实时捕获音频                波形跟随音量变化
              →  缓存音频数据                

松开 Alt+V    →  后端监听到释放              Island 状态变化
              →  发送 voice-mode-stop       显示 "🤔 Thinking..."
              →  停止录音                    波形动画停止
              →  音频预处理                  
              →  Whisper 语音识别           
              →  检测语言                    
              →  Qwen 翻译                   
              →  模拟键盘输入                翻译文本自动输入
              
完成          →  Island 消失                 恢复 idle 状态
```

#### 当前实现状态

✅ **已实现**:
- 全局快捷键监听 (Alt+V)
- 按下/释放事件检测
- 事件发送到前端
- Island UI 状态切换
- 波形动画效果

⚠️ **占位符实现**:
- 音频录制和处理
- Whisper 语音识别
- Qwen 翻译
- 键盘输入模拟

#### 使用场景

- 写邮件时快速输入翻译
- 聊天时用另一种语言回复
- 文档编辑时语音输入翻译

---

### 2. 文本翻译模式 (Alt+C)

#### 完整流程

```
用户操作          系统响应                    UI 反馈
─────────────────────────────────────────────────────────
选中文本      →  用户手动选中文本            

按 Alt+C      →  后端监听到按键              
              →  发送 text-mode-trigger     
              →  模拟 Ctrl+C                
              →  等待 100ms                  
              →  读取剪贴板                  
              →  备份原剪贴板                
              →  检测语言                    
              →  Qwen 翻译                   Glow 窗口出现
              →  显示翻译预览                显示翻译结果

查看翻译      →  用户阅读翻译                流光边框动画
              →  10秒倒计时                  显示倒计时

按 Enter      →  确认替换                    Glow 窗口消失
              →  将翻译写入剪贴板            
              →  模拟 Ctrl+V                
              →  恢复原剪贴板                翻译文本替换原文

或按 Esc      →  取消翻译                    Glow 窗口消失
              →  恢复原剪贴板                保持原文不变
```

#### 当前实现状态

✅ **已实现**:
- 全局快捷键监听 (Alt+C)
- 按下事件检测
- 事件发送到前端
- Glow UI 预览窗口
- Enter/Esc 键盘监听
- 10秒自动关闭
- 流光边框动画

⚠️ **占位符实现**:
- 剪贴板操作 (复制/粘贴)
- Qwen 翻译
- 键盘模拟 (Ctrl+C, Ctrl+V)

#### 使用场景

- 阅读外文文档时快速翻译
- 修改已有文本的语言
- 翻译选中的代码注释

---

### 3. 设置界面

#### 打开方式

- 点击右上角设置按钮 ⚙️
- 或从系统托盘菜单选择"设置"

#### 可配置项

- **快捷键自定义**
  - 语音模式快捷键 (默认: Alt+V)
  - 文本模式快捷键 (默认: Alt+C)
  
- **翻译方向**
  - 自动检测 (默认)
  - 中文 → 英文
  - 英文 → 中文
  
- **UI 设置**
  - 透明度调节
  - 动画速度调节

---

## 🎨 UI 组件说明

### Island 组件

**位置**: 屏幕顶部中央  
**尺寸**: 200px × 60px  
**效果**: 毛玻璃 + 弹簧动画

**状态**:
- 🎤 **Idle**: 准备就绪
- 🔴 **Recording**: 录音中（波形动画）
- 🤔 **Thinking**: 处理中
- ⚠️ **Error**: 错误提示

### Glow 组件

**位置**: 屏幕中央  
**尺寸**: 最大 600px × 400px  
**效果**: 毛玻璃 + 流光边框

**功能**:
- 显示翻译预览
- Enter 确认替换
- Esc 取消翻译
- 10秒自动关闭

### Settings 组件

**位置**: 全屏覆盖  
**效果**: 毛玻璃背景

**功能**:
- 快捷键配置
- 翻译设置
- UI 自定义

### ModelSetup 组件

**位置**: 全屏覆盖  
**效果**: 毛玻璃背景 + 滚动

**功能**:
- 检查模型状态
- 显示下载指引
- 一键复制命令

---

## 🔧 技术实现

### 后端事件

```rust
// 语音模式
app.emit("voice-mode-start", ());  // 按下 Alt+V
app.emit("voice-mode-stop", ());   // 松开 Alt+V

// 文本模式
app.emit("text-mode-trigger", ()); // 按下 Alt+C

// 错误
app.emit("error", error_message);  // 发生错误
```

### 前端监听

```typescript
// 监听语音模式开始
listen('voice-mode-start', () => {
  setState('recording');
});

// 监听语音模式结束
listen('voice-mode-stop', () => {
  setState('thinking');
});

// 监听文本模式触发
listen('text-mode-trigger', () => {
  showGlowWindow();
});
```

### Tauri Commands

```typescript
// 启动语音模式
await invoke('start_voice_mode');

// 启动文本模式
await invoke('start_text_mode');

// 确认翻译
await invoke('confirm_translation');

// 取消翻译
await invoke('cancel_translation');

// 获取配置
const config = await invoke('get_config');

// 更新配置
await invoke('update_config', { newConfig });

// 检查模型
const status = await invoke('check_models');
```

---

## 🎯 用户体验设计

### 无感交互

- **零学习成本**: 只需记住两个快捷键
- **即时反馈**: 所有操作都有视觉反馈
- **流畅动画**: 60fps 弹簧物理动画
- **智能检测**: 自动识别语言方向

### 视觉反馈

- **状态指示**: 清晰的图标和文字
- **进度提示**: 波形动画、倒计时
- **错误提示**: 友好的错误消息
- **成功确认**: 平滑的过渡动画

### 性能优化

- **快速启动**: < 5秒初始化
- **低延迟**: 语音识别 < 3秒
- **低资源**: 空闲 CPU < 1%
- **流畅动画**: 硬件加速渲染

---

## 📝 待完成功能

### 语音模式

- [ ] 实际音频录制
- [ ] Whisper 语音识别
- [ ] 音频质量检测
- [ ] 噪音过滤

### 文本模式

- [ ] 剪贴板操作实现
- [ ] 键盘模拟实现
- [ ] 格式保留逻辑
- [ ] 代码片段检测

### 翻译引擎

- [ ] Qwen 模型推理
- [ ] 翻译质量优化
- [ ] 上下文理解
- [ ] 专业术语处理

### 系统集成

- [ ] 窗口焦点管理
- [ ] 多显示器支持
- [ ] 权限请求优化
- [ ] 崩溃恢复

---

## 🚀 下一步

1. **下载模型文件** (必需)
   ```bash
   ./scripts/download-models.sh
   ```

2. **实现 ML 推理** (核心功能)
   - Whisper 语音识别
   - Qwen 文本翻译

3. **完善系统集成** (用户体验)
   - 剪贴板和键盘操作
   - 窗口管理
   - 错误处理

4. **测试和优化** (质量保证)
   - 端到端测试
   - 性能优化
   - 用户反馈

---

## 💡 提示

- 首次使用需要下载约 1.2GB 的模型文件
- 模型下载后完全离线运行
- 所有数据处理在本地完成，保护隐私
- 支持自定义快捷键，避免冲突

---

**当前版本**: v1.0.0  
**完成度**: 95% (等待模型文件)  
**状态**: 可运行，UI 完整，等待 ML 推理实现
